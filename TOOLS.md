# TOOLS.md

## Key Integrations
- Hevy API via `hevy-api-key`
- Home Assistant (Withings sensors) via `ha-token`

## Secrets Quick Commands
- `~/.local/bin/clawd-secrets status`
- `~/.local/bin/clawd-secrets get <key>`

## Note
Keep this file minimal. Move long examples to `docs/archive/`.
